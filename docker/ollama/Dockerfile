FROM ollama/ollama:0.3.6

# Add labels for GPU support information
LABEL com.reconpoint.gpu-support="optional"
LABEL com.reconpoint.gpu-description="GPU support can be enabled using docker-compose.gpu.yml"